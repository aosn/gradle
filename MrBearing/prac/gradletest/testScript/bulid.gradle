def message ='ローカル変数'
Stirng stringMessage = 'ローカル変数'

import java.sql.Timestamp

def data = new Data()
Timestamp ts = new Timestamp(data.getTime())

def scriptScope = 'SCRIPT'

task getMessage << {
    def taskScope = 'TASK'
    if(taskScope){
      def localScpoe = 'LOCAL'
      println scriptScope + ';' +taskScope +':'+ localScpoe
    }
}

println scriptScope
println taskScope
println localScpoe
